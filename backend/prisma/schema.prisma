// 1. Prisma Clientの設定
generator client {
  provider = "prisma-client-js"
}

// 2. データベースの接続設定
datasource db {
  provider = "postgresql"
  url      = env("LOCAL_DATABASE_URL")
}

// 3. ダミーのモデル(削除予定)
model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
}